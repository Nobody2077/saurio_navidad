# assets/images/

Coloca aquí el logo de la app:

- `logo.png` — logo principal (recomendado: 512x512 px, fondo transparente)

Cuando el archivo exista, registra la carpeta en pubspec.yaml:

```yaml
flutter:
  uses-material-design: true
  assets:
    - assets/images/
```
